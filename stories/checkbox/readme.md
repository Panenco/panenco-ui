## Usage

```js
import { Checkbox } from '@panenco/ui';

const render = () => {
  return (
    <Checkbox label='Checkbox' checked={isChecked} onClick={handleClick} error={error} disabled={isDisabled} />
```
