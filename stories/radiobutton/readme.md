## Usage

```js
import { Radio } from '@panenco/ui';
const render = () => {
  return (
    <Radio label='RadioButton' value="placeholder" checked={isChecked} onClick={handleClick} error={error} disabled={isDisabled} />
```
