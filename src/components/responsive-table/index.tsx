import { ResponsiveTable } from './table';
import type {
  ColumnType,
  RowType,
  SortType,
  HandleSortType,
  ExpandRowType,
  TableProps,
  CustomCellProps,
} from './types';

export { ResponsiveTable };
export type { ColumnType, RowType, SortType, HandleSortType, ExpandRowType, TableProps, CustomCellProps };
